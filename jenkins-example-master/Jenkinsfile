pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/Marvin1808/jenkinsPipeline.git'
            }
        }
        stage('Execute shell command') {
            steps {
                sh 'echo "Hello World!"'
            }
        }
    }
}
